{
  "name": "<%= props.name %>",
  "version": "0.0.1",
  "description": "",
  "author": "<%= props.author %>",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "eslint": "eslint --fix src test",
    "test": "istanbul cover node_modules/.bin/_mocha -- -u exports -R spec -r test/setup.js test/*{,*/}*.js",
    "watch": "nodemon --ignore out/ --ignore node_modules/ --exec 'npm run eslint && npm run test' ",<% if (props.isApp) { %>
    "server": "nodemon --ignore node_modules/ src/index.js",
    "start": "npm run watch & sleep 3000 && npm run server" <% } else { %>
    "start": "npm run watch"
    <% } %>
  }
}
